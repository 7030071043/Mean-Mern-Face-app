{
  "name": "mern-app",
  "version": "1.0.0",
   "scripts": {
  "build": "cd client && npm install && npm run build && rm -rf ../server/public && mv build ../server/public",
  "start": "cd server && npm install && npm start"
}
,
  "engines": {
    "node": "18.x"
  }
}
