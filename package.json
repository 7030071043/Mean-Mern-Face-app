{
  "name": "mern-face-recognition",
  "version": "1.0.0",
  "scripts": {
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "build": "npm run install-client && cd client && npm run build && rm -rf ../server/public && mv build ../server/public && npm run install-server",
    "start": "cd server && node app.js"
  },
  "engines": {
    "node": "18.x"
  }
}
